# SPDX-License-Identifier: GPL-3.0-or-later

# AuroraSMS currently relies on Android manifest entry-point retention and the
# consumer rules shipped by its audited AndroidX dependencies. Add only narrow,
# evidence-backed rules here; broad keep rules would invalidate release and
# Baseline Profile measurements.
